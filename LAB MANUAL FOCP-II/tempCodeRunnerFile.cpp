class volumefigure{

public:
float volume(float radius,float height)}
return((3.14*raduis*radiusxheight))

int volume(int side) {
return sidexsidexside; }

int volume(int length, int breadth, int height){
return( lengthxbreadthxheight);
}

int main()
{
float vol_cone;
int vol_cube;
int vol_cuboid;

volumefigure ob;
vol_cone= ob. volume(7.5,4.9);
vol_cube= ob. volume(9);
vol_cuboid= ob. volume(5,7,6);
cout<< "volume of cone is: << vol_cone;
cout<< "volume of cube is: << vol_cube;
cout<<"volume of cuboid is: << vol_cuboid is:"<< vol_cuboid:
 
return 0; 
}
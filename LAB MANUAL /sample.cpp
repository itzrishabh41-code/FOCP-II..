class sample
{
    int num;
    public:
    sample(int x=0)
    {
        num =x;
    }
    sample operator+(sample S1 ) // if user doesno pass rthe argyment the default argument will be takern by the compoler to initilized the value but if we are passing the arguent whiolw clling or creating object , that argument holds the higher priority than default argumrnt 
    {
        sample temp;
        temp num = num+
    }
}


